<p id="notice"><%= notice %></p>

<h1>Pets</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pets.each do |pet| %>
      <tr>
        <!-- link_to is an example of one of Rails' ActionView Helper methods. We pass it two arguments - the text for the link and then the URL and it will create a corresponding a tag. -->
        <td><%= link_to pet.name, pet %></td>
        <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
        <!-- Clicking on a link will make a GET request, but we can override the type of HTTP verb it uses by specifying the method. In this case, we have made it so that when a user clicks on this link, they are actually making a DELETE request. -->
        <td><%= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pet', new_pet_path %>
